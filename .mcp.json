{
  "mcpServers": {
    "gall": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "cd \"${GALL_HOME:-.}\" && exec nix develop .#default --command gleam run --module gall/daemon"
      ]
    }
  }
}
